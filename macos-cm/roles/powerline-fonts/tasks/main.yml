---
- name: Clone the repo
  git:
    repo: "https://github.com/powerline/fonts.git"
    dest: /tmp/fonts/
    depth: 1

- name: Install
  command: /tmp/fonts/install.sh

- name: Cleanup
  command: rm -rf /tmp/fonts