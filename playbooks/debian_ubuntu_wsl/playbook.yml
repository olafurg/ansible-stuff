---
- name: Set up Debian/Ubuntu WSL localhost
  hosts: localhost
  connection: local

  roles:
    - debian_base
    - oh_my_zsh
    - git
    - gh
    - vim
    - tmux
    - terraform
    - azure_cli
    - 1password_wsl
    # - asdf
    # - prometheus
    # - grafana
    # - node_exporter
