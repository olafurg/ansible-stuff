---
- name: Set up Arch Linux localhost
  hosts: localhost
  connection: local

  tasks:
    - name: Install base packages
      become: true
      pacman:
        name:
          - bind # DNS resolver
          - fastfetch
          - git
          - htop
          - zsh
        state: present
        update_cache: false

  # roles:
  #   - oh_my_zsh
  #   - git
  #   - gh
  #   - vim
  #   - tmux
  #   - terraform
  #   - azure_cli
  #   - 1password_wsl
    # - asdf
    # - prometheus
    # - grafana
    # - node_exporter
